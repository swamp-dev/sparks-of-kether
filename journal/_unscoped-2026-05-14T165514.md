# Journal — unscoped: hand magnify center + scale + fan pan

Append-only. Never edit or delete past entries. One entry per `git push`
on this branch.

---

## 2026-05-14T16:55:14-04:00 — push 1 hand magnify centre, scale, fan pan

**Pushed:** fix(hand): centre magnified card on screen, reduce scale, add fan pan — MAGNIFY_SCALE_BIG 3.5→1.5, MAGNIFY_LIFT_VH 35→20; useLayoutEffect centering; fan horizontal drag.
**Why:** Magnified cards were lifting in-place at their fan position (edge cards flew off-screen); 3.5× scale made cards wall-sized; fan cards behind others were hard to reach.
**Notes:** Used `card.offsetLeft` (layout-only, transform-free) rather than `getBoundingClientRect()` for the centering measurement — `getBoundingClientRect` would include the stale `magnifyOffsetX` from the previously-hovered card when sliding A→B, compounding the error. `overflow-x-clip` moved from inner fan div to outer fixed wrapper so the clip boundary stays fixed while the fan translates. Fan pan has `e.target === e.currentTarget` guard so card drag-to-play paths are unaffected. No ticket; work done directly without `/start-ticket`. Pre-existing music synth timeout in local test run (passes in CI).
**Commit(s):** `9aab1bb`
