# Journal — Blessing ritual: avatar portrait + animated d6 dice

Append-only. Never edit or delete past entries. One entry per `git push`
on this branch.

---

## 2026-05-15T00:21:15+00:00 — initial implementation + review fixes

**Pushed:** Two commits. (1) feat: replace SefirahHero orb with AvatarPortrait (stage size),
add D6 SVG token + D6Roll RAF-animated wrapper, widen page to max-w-5xl, switch to
3-column avatar|ceremony|ledger grid. Update 5 BlessingRitual tests to match new design.
(2) fix: nested aria-live suppression via `announceToAt={false}` in RollDisplay, pip array
type safety (`?? []`), add comment on D6Roll `settled` divergence from D20Roll.
**Why:** User wanted avatar instead of Hebrew letter orb, animated d6 dice matching encounter
d20 style, and better use of page vertical space. Code-reviewer flagged nested live regions —
fixed before PR open.
**Notes:** `rolling` flag is decorative-only (Next button not gated on animation), so existing
tests with immediate post-roll `fireEvent.click(Next)` continue working. kether/malkuth fall
back to Hebrew letter placeholder via AvatarPortrait's existing `imageFailed` logic.
**Commit(s):** `4886278..09a39e2`
