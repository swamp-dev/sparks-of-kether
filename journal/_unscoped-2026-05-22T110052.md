# Journal — unscoped: Hebrew board redesign

Append-only. Never edit or delete past entries. One entry per `git push`
on this branch.

---

## 2026-05-22T11:00:52-04:00 — initial push

**Pushed:** Hebrew script on Sephirot nodes + visual enhancements to the Tree board.
**Why:** User requested Hebrew names (כתר, תפארת, etc.) replace Latin transliterations (KETHER, TIFERET, etc.) and a more mesmerizing board aesthetic.
**Notes:** Reverses deliberate #401 decision to show transliterations. Three visual layers added: radial gradient gleam (luminous orb effect), inner decorative ring (bezel), expanded starfield (12→24 stars with size variety). Font changed from Fraunces `--font-display` to Frank Ruhl Libre `--font-hebrew`, weight 600→700. `fontSizeForName` replaced by `fontSizeForHebrew` with Hebrew-calibrated size buckets. ArcanumCard snapshots updated (pre-existing staged changes from a prior session). Code-reviewer run; two significant findings fixed (stale inline comment, orphaned JSDoc block). No ticket — user-initiated from plan mode.
**Commit(s):** see below
