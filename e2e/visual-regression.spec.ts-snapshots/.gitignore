# CI runs ubuntu-latest, so the linux-suffixed baselines are the
# only ones the assertion compares against. Mac (`*-darwin.png`)
# and Windows (`*-win32.png`) baselines generated by contributors
# on those platforms would coexist silently and never be checked,
# adding dead weight to the repo. Block them at commit time.
*-darwin.png
*-win32.png
